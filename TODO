Add option to search plays or poems or sonnets

Add option to download all the texts

Factor out fetch(`${HTML_DIR}

Add line numbers for offset lines only once.

Add exact match only

Solve problem of non-found scenelocations
